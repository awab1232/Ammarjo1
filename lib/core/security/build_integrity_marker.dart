/// Set to `true` after production hardening (no Firestore outside communication,
/// no silent backend fallbacks in hardened paths).
const bool PRODUCTION_HARDENED = true; // ignore: constant_identifier_names
