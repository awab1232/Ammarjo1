/**
 * Policy parity with Flutter `lib/core/security/firestore_killer.dart`:
 * Firestore is not used by this Nest API; data lives in PostgreSQL.
 * This side-effect import keeps a searchable anchor in the backend bootstrap.
 */
export {};
